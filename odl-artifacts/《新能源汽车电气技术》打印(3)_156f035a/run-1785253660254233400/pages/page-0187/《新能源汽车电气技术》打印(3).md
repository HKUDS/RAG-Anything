![](<《新能源汽车电气技术》打印(3)_images/imageFile1.png>)

图 5-7 CAN 总线数据传输过程 CAN 总线所传输的信息也称为报文，是一帧一帧的传送，每帧数据由一组二进制数组成，

这组二进制数按功能分为一段一段的，每一段称为帧的域或区。CAN 总线所传输的数据有数 据帧、远程帧、错误帧和过载帧四种类型。数据帧是用于 CAN 节点单元之间进行数据传输的 帧，它由开始域、仲裁域、控制域、数据域、安全域、确认域和结束域 7 个域组成，如图 5-8 所示。

![](<《新能源汽车电气技术》打印(3)_images/imageFile2.png>)

图 5-8 数据帧格式

在 CAN 总线系统的所有控制单元都能发送信息，如果多个控制单元同时发送信息，那么 数据总线上就会发生数据冲突。为了避免发生这种情况，CAN 总线具有冲突仲裁机制。按照 信息的重要程度分配优先权，紧急的信息优先权高，不是特别紧急的信息优先权低，确保优 先权高的信息能够优先发送。CAN 总线系统的仲裁机制如下:

- 1）每个控制单元在发送信息时，通过发送标识符来标识信息类别，信息优先权包含在

标识符中，标识符位于数据格式的状态域。

- 2）所有控制单元都通过各自的 RX 线来跟踪总线上的信息并获知总线状态。


187

