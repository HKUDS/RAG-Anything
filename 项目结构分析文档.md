
# RAG-Anything 项目结构分析文档

## 一、项目概述

**RAG-Anything** 是一个基于 LightRAG 构建的**全功能多模态文档处理 RAG（检索增强生成）系统**。该项目旨在解决传统文本聚焦的 RAG 系统无法有效处理多样化多模态内容（文本、图像、表格、公式、图表等）的问题。

### 项目定位
- **核心价值**：提供端到端的多模态文档处理和查询能力
- **技术基础**：基于香港大学数据科学学院的 LightRAG 框架
- **支持格式**：PDF、Office 文档（DOC/DOCX/PPT/PPTX/XLS/XLSX）、图像、文本文件等

---

## 二、项目目录结构

```
RAG-Anything/
├── .github/                    # GitHub 配置文件
│   ├── ISSUE_TEMPLATE/         # Issue 模板
│   ├── workflows/              # CI/CD 工作流
│   ├── dependabot.yml          # 依赖更新配置
│   └── pull_request_template.md # PR 模板
├── assets/                     # 资源文件
├── docs/                       # 文档说明
├── examples/                   # 使用示例
├── my_docs/                    # 用户文档目录
├── raganything/                # 核心源代码
├── reproduce/                  # 复现实验代码
├── scripts/                    # 辅助脚本
├── tests/                      # 测试文件
└── 配置文件                    # pyproject.toml, requirements.txt 等
```

---

## 三、核心模块详解

### 3.1 主代码目录：`raganything/`

| 文件 | 功能描述 |
|------|----------|
| `__init__.py` | 包初始化，导出核心类和函数 |
| `raganything.py` | 主类 `RAGAnything`，整合所有功能 |
| `config.py` | 配置类，支持环境变量配置 |
| `parser.py` | 文档解析器（MinerU/Docling/PaddleOCR） |
| `processor.py` | 文档处理功能（解析、多模态处理） |
| `query.py` | 查询功能（文本查询、多模态查询、VLM增强查询） |
| `batch.py` | 批量处理功能 |
| `batch_parser.py` | 批量解析器 |
| `modalprocessors.py` | 多模态处理器（图像、表格、公式） |
| `prompt.py` | 提示词模板 |
| `prompt_manager.py` | 多语言提示词管理器 |
| `prompts_zh.py` | 中文提示词模板 |
| `callbacks.py` | 回调管理器（可观测性和指标） |
| `resilience.py` | 弹性机制（重试、熔断） |
| `utils.py` | 工具函数 |
| `base.py` | 基础类和状态定义 |
| `omml_extractor.py` | OMML 公式提取器 |
| `asset_urls.py` | 资源 URL 管理 |

#### 3.1.1 核心类 `RAGAnything`

**继承结构**：
```python
class RAGAnything(QueryMixin, ProcessorMixin, BatchMixin):
```

**核心职责**：
1. **初始化 LightRAG**：创建或接收预初始化的 LightRAG 实例
2. **管理多模态处理器**：图像、表格、公式处理器的初始化和调度
3. **上下文提取**：为多模态内容提供上下文信息
4. **配置管理**：支持运行时配置更新

#### 3.1.2 配置系统：`RAGAnythingConfig`

**配置类别**：

| 配置类别 | 主要参数 | 说明 |
|----------|----------|------|
| **目录配置** | `working_dir`, `parser_output_dir` | 工作目录和解析输出目录 |
| **解析配置** | `parser`, `parse_method`, `display_content_stats` | 解析器选择和解析方法 |
| **多模态处理** | `enable_image_processing`, `enable_table_processing`, `enable_equation_processing` | 启用/禁用各类多模态处理 |
| **批量处理** | `max_concurrent_files`, `supported_file_extensions`, `recursive_folder_processing` | 并发数、支持格式、递归处理 |
| **上下文提取** | `context_window`, `context_mode`, `max_context_tokens`, `include_headers`, `include_captions` | 上下文窗口大小和提取模式 |

#### 3.1.3 多模态处理器：`modalprocessors.py`

| 处理器 | 功能 | 依赖模型 |
|--------|------|----------|
| `ContextExtractor` | 通用上下文提取器 | - |
| `ImageModalProcessor` | 图像内容处理 | VLM（视觉语言模型） |
| `TableModalProcessor` | 表格内容处理 | LLM |
| `EquationModalProcessor` | 数学公式处理 | LLM |
| `GenericModalProcessor` | 通用处理器（作为后备） | LLM |

#### 3.1.4 查询功能：`query.py`

**查询模式**：
1. **纯文本查询**：`aquery()` - 直接调用 LightRAG 的查询功能
2. **多模态查询**：`aquery_with_multimodal()` - 结合文本和多模态内容
3. **VLM增强查询**：`aquery_vlm_enhanced()` - 自动分析检索到的图像内容

---

### 3.2 文档目录：`docs/`

| 文件 | 内容 |
|------|------|
| `batch_processing.md` | 批量处理指南 |
| `context_aware_processing.md` | 上下文感知处理说明 |
| `enhanced_markdown.md` | 增强型 Markdown 格式说明 |
| `multimodal_rag_failure_modes.md` | 多模态 RAG 失败模式分析 |
| `offline_setup.md` | 离线环境配置指南 |
| `vllm_integration.md` | vLLM 集成说明 |

---

### 3.3 示例目录：`examples/`

| 文件 | 功能演示 |
|------|----------|
| `raganything_example.py` | 基础使用示例 |
| `batch_processing_example.py` | 批量处理示例 |
| `batch_dry_run_example.py` | 批量处理预演示例 |
| `enhanced_markdown_example.py` | 增强型 Markdown 示例 |
| `image_format_test.py` | 图像格式测试 |
| `office_document_test.py` | Office 文档测试 |
| `text_format_test.py` | 文本格式测试 |
| `ollama_integration_example.py` | Ollama 集成示例 |
| `lmstudio_integration_example.py` | LM Studio 集成示例 |
| `minimax_integration_example.py` | MiniMax 集成示例 |
| `vllm_integration_example.py` | vLLM 集成示例 |
| `modalprocessors_example.py` | 多模态处理器示例 |
| `insert_content_list_example.py` | 直接插入内容列表示例 |

---

### 3.4 CI/CD 配置：`.github/workflows/`

| 文件 | 功能 |
|------|------|
| `linting.yaml` | 代码风格检查（ruff） |
| `test.yaml` | 自动化测试 |
| `pypi-publish.yml` | PyPI 发布流程 |

---

## 四、技术架构

### 4.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                     RAG-Anything 系统架构                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐                                               │
│  │   文档输入    │  PDF, Office, Image, Text                    │
│  └──────┬───────┘                                               │
│         │                                                       │
│         ▼                                                       │
│  ┌──────────────┐                                               │
│  │   文档解析    │  MinerU / Docling / PaddleOCR                │
│  │    (Parser)   │  支持 OCR/文本/自动模式                       │
│  └──────┬───────┘                                               │
│         │                                                       │
│         ▼                                                       │
│  ┌──────────────────────────────┐                               │
│  │        内容分离              │                               │
│  │  ┌─────┬──────┬──────┬─────┐│                               │
│  │  │文本 │ 图像  │ 表格  │ 公式 ││                               │
│  │  └──┬──┴──┬───┴──┬───┴──┬──┘│                               │
│  └─────┼─────┼──────┼──────┼────┘                               │
│        │     │      │      │                                    │
│        │     ▼      ▼      ▼                                    │
│        │  ┌─────────────────────┐                               │
│        │  │   多模态处理器        │  Image/Table/Equation        │
│        │  │  (ModalProcessors)   │  处理器                       │
│        │  └───────────┬─────────┘                               │
│        │              │                                         │
│        ▼              ▼                                         │
│  ┌──────────────────────────────┐                               │
│  │        LightRAG 核心         │  知识图谱 + 向量数据库          │
│  │  ┌────────────────────────┐ │                               │
│  │  │ 实体提取 | 关系抽取     │ │                               │
│  │  │ 向量检索 | 图遍历       │ │                               │
│  │  └────────────────────────┘ │                               │
│  └──────────────┬───────────────┘                               │
│                 │                                               │
│                 ▼                                               │
│  ┌──────────────┐                                               │
│  │    查询接口   │  aquery / aquery_with_multimodal             │
│  │              │  aquery_vlm_enhanced                          │
│  └──────────────┘                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 核心处理流程

#### 文档处理流程
1. **文档解析**：使用 MinerU/Docling/PaddleOCR 解析文档
2. **内容分类**：分离文本、图像、表格、公式等内容类型
3. **文本处理**：直接插入 LightRAG
4. **多模态处理**：调用专用处理器生成描述和实体信息
5. **知识图谱构建**：提取实体和关系，构建知识图谱

#### 查询流程
1. **文本查询**：直接调用 LightRAG 查询
2. **多模态查询**：先处理多模态内容生成描述，再进行查询
3. **VLM增强**：自动检测检索结果中的图像，使用 VLM 进行分析

---

## 五、依赖与环境

### 5.1 核心依赖

| 依赖 | 版本 | 作用 |
|------|------|------|
| `lightrag-hku` | <1.5 | RAG 核心框架 |
| `mineru[core]` | - | 文档解析引擎 |
| `huggingface_hub` | - | HuggingFace Hub 访问 |
| `tqdm` | - | 进度条显示 |

### 5.2 可选依赖

| 依赖组 | 包含包 | 功能 |
|--------|--------|------|
| `[image]` | Pillow | BMP/TIFF/GIF/WebP 支持 |
| `[text]` | reportlab | TXT/MD 文件处理 |
| `[paddleocr]` | paddleocr, pypdfium2 | OCR 支持 |
| `[markdown]` | markdown, weasyprint, pygments | Markdown 转 PDF |
| `[all]` | 全部可选依赖 | 完整功能 |

### 5.3 外部依赖

| 软件 | 用途 | 安装要求 |
|------|------|----------|
| **LibreOffice** | Office 文档转 PDF | 必须单独安装 |
| **MinerU** | 文档解析引擎 | 通过 pip 安装 |

---

## 六、关键配置与环境变量

### 6.1 环境变量配置

| 环境变量 | 说明 | 默认值 |
|----------|------|--------|
| `WORKING_DIR` | RAG 存储和缓存目录 | `./rag_storage` |
| `PARSE_METHOD` | 解析方法（auto/ocr/txt） | `auto` |
| `OUTPUT_DIR` | 解析输出目录 | `./output` |
| `PARSER` | 解析器（mineru/docling/paddleocr） | `mineru` |
| `ENABLE_IMAGE_PROCESSING` | 启用图像处理 | `true` |
| `ENABLE_TABLE_PROCESSING` | 启用表格处理 | `true` |
| `ENABLE_EQUATION_PROCESSING` | 启用公式处理 | `true` |
| `MAX_CONCURRENT_FILES` | 最大并发处理文件数 | `1` |
| `CONTEXT_WINDOW` | 上下文窗口大小 | `1` |
| `CONTEXT_MODE` | 上下文模式（page/chunk） | `page` |
| `MAX_CONTEXT_TOKENS` | 最大上下文 tokens | `2000` |

---

## 七、使用模式

### 7.1 基本使用流程

```python
from raganything import RAGAnything, RAGAnythingConfig

# 1. 创建配置
config = RAGAnythingConfig(
    working_dir="./rag_storage",
    parser="mineru",
    enable_image_processing=True,
    enable_table_processing=True,
    enable_equation_processing=True,
)

# 2. 初始化 RAGAnything（提供 LLM、VLM、Embedding 函数）
rag = RAGAnything(
    config=config,
    llm_model_func=llm_model_func,
    vision_model_func=vision_model_func,
    embedding_func=embedding_func,
)

# 3. 处理文档
await rag.process_document_complete(
    file_path="document.pdf",
    output_dir="./output"
)

# 4. 查询
result = await rag.aquery("文档的主要结论是什么？", mode="hybrid")
```

### 7.2 批量处理

```python
# 处理文件夹中的所有文档
await rag.process_folder_complete(
    folder_path="./documents",
    output_dir="./output",
    file_extensions=[".pdf", ".docx"],
    recursive=True,
    max_workers=4
)
```

### 7.3 多模态查询

```python
# 查询时携带图像内容
result = await rag.aquery_with_multimodal(
    "分析这张图中的数据",
    multimodal_content=[{
        "type": "image",
        "img_path": "./figure.jpg"
    }],
    mode="hybrid"
)
```

---

## 八、扩展能力

### 8.1 自定义解析器注册

```python
from raganything.parser import register_parser

# 注册自定义解析器
register_parser("my_parser", MyCustomParser)

# 使用自定义解析器
config = RAGAnythingConfig(parser="my_parser")
```

### 8.2 自定义多模态处理器

```python
from raganything.modalprocessors import GenericModalProcessor

class CustomModalProcessor(GenericModalProcessor):
    async def process_multimodal_content(self, modal_content, content_type, file_path, entity_name):
        # 自定义处理逻辑
        enhanced_description = await self.analyze_custom_content(modal_content)
        entity_info = self.create_custom_entity(enhanced_description, entity_name)
        return await self._create_entity_and_chunk(enhanced_description, entity_info, file_path)
```

### 8.3 回调机制

```python
from raganything.callbacks import ProcessingCallback, CallbackManager

class MyCallback(ProcessingCallback):
    def on_parse_start(self, **kwargs):
        print(f"开始解析: {kwargs['file_path']}")
    
    def on_query_complete(self, **kwargs):
        print(f"查询完成，耗时: {kwargs['duration_seconds']}s")

# 注册回调
callback_manager = CallbackManager()
callback_manager.register(MyCallback())

# 在 RAGAnything 中使用
rag = RAGAnything(...)
rag.callback_manager = callback_manager
```

---

## 九、测试与验证

### 9.1 测试文件

| 文件 | 测试内容 |
|------|----------|
| `tests/testpaddleocr_parser.py` | PaddleOCR 解析器测试 |
| `tests/testparser_kwargs.py` | 解析器参数测试 |
| `tests/testparser_wiring.py` | 解析器集成测试 |

### 9.2 运行测试

```bash
# 使用 uv 运行测试
uv run pytest tests/

# 指定测试文件
uv run pytest tests/testparser_kwargs.py -v
```

---

## 十、部署与分发

### 10.1 安装方式

**方式一：从 PyPI 安装**
```bash
pip install raganything
pip install 'raganything[all]'  # 包含所有可选依赖
```

**方式二：从源码安装**
```bash
git clone https://github.com/HKUDS/RAG-Anything.git
cd RAG-Anything
uv sync
```

### 10.2 发布流程

项目通过 GitHub Actions 自动发布到 PyPI，触发条件：
1. 创建新的 Git 标签
2. 手动触发 `pypi-publish.yml` 工作流

---

## 十一、总结

### 项目亮点

1. **端到端多模态处理**：从文档解析到智能查询的完整流程
2. **灵活的解析器架构**：支持多种解析器切换（MinerU/Docling/PaddleOCR）
3. **上下文感知处理**：智能提取多模态内容的上下文信息
4. **VLM增强查询**：自动分析检索结果中的图像内容
5. **可扩展架构**：支持自定义解析器、处理器和回调
6. **弹性机制**：内置重试和熔断支持

### 适用场景

- **学术研究**：处理包含公式、图表的论文
- **技术文档**：处理技术手册、API文档
- **金融报告**：分析包含表格和图表的财务报表
- **企业知识管理**：构建多模态企业知识库

---

**文档版本**：v1.0  
**生成日期**：2025年  
**项目地址**：https://github.com/HKUDS/RAG-Anything
