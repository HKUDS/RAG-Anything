## 1. 图谱实体匹配升级 (graph_rag.py)

- [x] 1.1 修改 `GraphRetriever._match_entities()` 方法：新增基于 jieba token 重叠度的加权匹配逻辑。对每个实体名称和查询分别使用 jieba 分词，计算 token 交集大小作为匹配得分。保留原有子串匹配作为兜底（token 重叠为 0 但子串匹配的实体仍纳入结果，score=0.5）
- [x] 1.2 修改实体排序逻辑：先按 token 重叠 score 降序，同分按 graph degree 降序。确保更特定的实体（如"毕业设计答辩"）排在更通用的实体（如"开题答辩"）前面
- [x] 1.3 修改 `_traverse_neighbors()` 方法：在返回值中增加 chunk_id 到来源实体名称的映射（`chunk_entity_map: dict[str, list[str]]`），记录每个 chunk 是通过哪些实体遍历到的

## 2. 检索上下文标注实体来源 (query.py)

- [x] 2.1 修改 `QueryMixin._aquery_rrf()` 中图谱通道结果的上下文构建逻辑（约第 581–587 行）：读取 `chunk_entity_map`，对每个来自图谱通道的 chunk，在其文本前添加 `[来源实体: {entity_name}]` 标注前缀
- [x] 2.2 确保当同一 chunk 来自多个实体时，标注所有来源实体名称（如 `[来源实体: 开题答辩, 毕业设计答辩]`）
- [x] 2.3 验证向量通道和 BM25 通道的 chunk 不受影响（不加标注前缀）

## 3. RAG 提示词增强 (server.py)

- [x] 3.1 修改 Agent 流式查询中的 RAG 提示词（`server.py` 第 2244 行附近）：在"要求"部分增加实体消歧指令
- [x] 3.2 修改非流式查询 `/api/query` 中的 RAG 提示词（`server.py` 第 2507–2517 行附近）：同步增加实体消歧指令

## 4. 集成验证

- [ ] 4.1 使用现有文档"13.相关文件"验证：查询"毕业设计答辩地点是哪里" → 确认返回 13216 而非 13220
- [ ] 4.2 查询"开题答辩地点是哪里" → 确认返回 13220
- [ ] 4.3 查询"开题答辩和毕业答辩的地点一样吗" → 确认 LLM 回答不一样，并分别列出两个地点
- [ ] 4.4 验证非消歧场景不受影响：通用查询（如"这篇文档的主要内容"）仍正常返回结果
